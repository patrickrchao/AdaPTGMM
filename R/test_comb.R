#
# interval <- FALSE
# p_values <- c(0.01,0.03)
# x <- c(0,0)
# tent <- TRUE
# # all_data <- generate_data(500000,num_df= 1,num_classes = 2,interval=interval)
# # known <- all_data$known
# # x <- known$x
# # p_values <- known$p_values
# output <- AdaPTGMM(x,p_values=p_values,interval=interval,num_df=1,
#                    iterations=0,alpha_m = 0.1,zeta = 0.2,lambda=0.4,
#                    tent=tent,num_classes=2,selection="NONE")
